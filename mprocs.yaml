procs:
  frontend:
    shell: "sbtn '~frontend/fastLinkJS'"
    autorestart: true
  backend:
    shell: "sbtn '~devServer/reStart all'"
    autorestart: true
  vite: 
    shell: "npm run dev"
    cwd: "modules/frontend"
    autorestart: true
